apiVersion: v1
kind: Service
metadata:
   name: result-svc
   labels:
     app: voting-app
     type: result-svc

spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      
  selector: 
      type: result-pod
      app: voting-app